# UUON Compression Field Engine — Environment Variables
#
# The browser engine (index.html) requires NONE of these.
# This file documents what a backend API server would need.

PORT=3000
CORS_ORIGIN=https://uuon-foundation.github.io
ENGINE_CORE_PATH=engine/compression-field/core.js
STORAGE_BUCKET=
DATABASE_URL=
